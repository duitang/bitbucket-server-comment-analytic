<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
    </plugin-info>

    <component-import key="git-scm" interface="com.atlassian.stash.scm.git.GitScm" />
    <component-import key="i18nService" interface="com.atlassian.stash.i18n.I18nService" />

    <component key="archive-service" class="com.atlassian.stash.archive.ArchiveService" />

    <rest key="archive-rest" path="/archive" version="1.0">
        <dispatcher>REQUEST</dispatcher>
        <dispatcher>FORWARD</dispatcher>
        <init-param>
            <param-name>extension.filter.excludes</param-name>
            <param-value>.*</param-value>
        </init-param>
        <package>com.atlassian.stash.archive.rest</package>
    </rest>

</atlassian-plugin>
